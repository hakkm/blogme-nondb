<%- include("header") -%>
  <h1><%= title %></h1>
  <p><%= content %></p>

  <% for (let post of postList) { %>
    <h2><%= post.title %></h2>
    <p><%= post.content.substring(0, 100) + " ..."; %> <a href="/posts/<%= post.title %>">Read More</a></p>
  <% } %>
<%- include("footer") -%>
